# Coding Standards (Initial)

- Prefer small, reversible changes.
- Respect module boundaries; avoid cross-module coupling.
- Keep configuration externalized; avoid hard-coded secrets or provider-specific assumptions.
- Require explicit permission checks for sensitive actions.
- Ensure errors are structured and observable.
- Add tests for new behavior and regression risk.
- Avoid unnecessary rewrites; refactor only with justification.
