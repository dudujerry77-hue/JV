{
  "project": "Jarvis",
  "governance_version": "0.1.0",
  "last_updated": "2026-08-13",
  "current_phase": "Phase 2 - Voice",
  "phase_status": "in_progress",
  "architecture_version": "0.1-draft",
  "repository_state": {
    "application_code_exists": true,
    "technology_stack_decided": true,
    "notes": "Phase 1 Foundation complete and verified (see phase_completion_records/P001-completion.md). Phase 2 Voice: round 1 real-hardware test on owner's EliteBook found audio/TTS/STT PASS but wake-word detection FAIL. Root-caused to inference running inside a real-time audio callback (D-0016) and fixed via a queue-decoupled MicrophoneChunkStream; 52 automated tests passing. NOT YET RE-VERIFIED on real hardware -- awaiting round 2 results before Phase 2 can be marked verified."
  },
  "implemented_features": [
    "Layered config system (defaults -> YAML file -> env overrides), validated via Pydantic (jarvis_core/config)",
    "Structured logging to console + rotating file (jarvis_core/observability)",
    "SQLite storage layer with permission_grants and audit_log tables (jarvis_core/storage)",
    "Capability-based permission system: deny-by-default store, tier classification, require() enforcement gate with audit logging (jarvis_core/permissions)",
    "Plugin manifest schema + directory-based discovery with per-plugin failure isolation (jarvis_core/plugins)",
    "Core HTTP service (FastAPI/Uvicorn, loopback-only, enforced) with /health and /status endpoints (jarvis_core/service)",
    "Voice engine interfaces (Protocol-based, swappable backends) (jarvis_core/voice/engines.py)",
    "Text-to-speech via pyttsx3/OS voice engine (jarvis_core/voice/tts.py)",
    "Speech-to-text via faster-whisper, lazy model loading (jarvis_core/voice/stt.py)",
    "Wake-word detection via openWakeWord, lazy model loading (jarvis_core/voice/wakeword.py)",
    "Real microphone/speaker I/O adapter via sounddevice (jarvis_core/voice/audio_io.py)",
    "Echo conversation pipeline + wake-word-gated listen loop (jarvis_core/voice/pipeline.py)",
    "MICROPHONE-permission-gated voice pipeline assembly (jarvis_core/voice/factory.py)"
  ],
  "verified_features": [
    "Config system: 6 automated tests passing (defaults, file override, env override, path resolution, voice config defaults/override)",
    "Permission system: 8 automated tests passing (deny-by-default, grant/revoke, full-catalog seeding, tier mapping, require() allow/deny paths, audit-log write)",
    "Plugin discovery/registry: 6 automated tests passing (empty dir, missing dir, valid manifest, invalid manifest isolation, no-manifest dir skipped, registry register/get/list)",
    "Core service: 8 automated tests passing (in-process /health and /status, loopback-enforcement allow/reject cases), plus one manual real-socket smoke test",
    "Phase 1 -- Foundation: verified complete per roadmap.md's bar. See phase_completion_records/P001-completion.md.",
    "Text-to-speech (jarvis_core/voice/tts.py): genuinely verified end-to-end in-sandbox -- real audio files produced via the OS voice engine, 2 tests passing.",
    "Voice wrapper logic only (NOT real model behavior): speech-to-text lazy-loading/joining logic (4 tests), wake-word lazy-loading/threshold logic (6 tests), echo pipeline + listen-loop state machine (4 tests), audio file I/O format correctness (2 tests), capture-callback queue-decoupling contract (3 tests), MICROPHONE-permission gating (3 tests) -- all against mocked model layers or hardware-free file I/O.",
    "Round 1 real-hardware results (owner, EliteBook 645 G9): audio devices PASS, TTS PASS, STT PASS, wake-word detection FAIL. Root cause found and fixed (D-0016) -- inference was running inside a real-time audio callback, which can silently drop input. NOT yet re-verified on real hardware -- Phase 2 remains unverified pending round 2."
  ],
  "blocked_features": [],
  "active_tasks": [],
  "latest_decisions": [
    "D-0001",
    "D-0002",
    "D-0003",
    "D-0004",
    "D-0005",
    "D-0006",
    "D-0007",
    "D-0008",
    "D-0009",
    "D-0010",
    "D-0011",
    "D-0012",
    "D-0013",
    "D-0014",
    "D-0015",
    "D-0016"
  ],
  "known_risks": [
    "Primary device (HP EliteBook 645 G9) has no dedicated GPU, so the local-tier/cloud-tier split (D-0006) is a hard constraint on later phases, not a preference.",
    "No first-party plugins exist yet, so plugin isolation has only been tested against synthetic fixtures, not a real plugin -- plugin *execution* (vs. manifest loading) is unbuilt and unreviewed.",
    "The core service has no authentication yet; it is only safe because of the loopback-only enforcement (D-0013) -- must be revisited before any phase exposes it beyond localhost.",
    "Android integration approach and specific cloud AI provider(s)/cost ceiling remain open, deferred to Phase 7 and Phase 3 respectively.",
    "Round 1 hardware test found wake-word detection failing on the owner's real machine. Root cause fixed (D-0016: audio capture/inference decoupling) but NOT yet re-verified on real hardware -- do not assume the fix works until round 2 confirms it."
  ],
  "next_actions": [
    "Owner pulls this fix and re-runs scripts/verify_phase2_voice.py on the EliteBook 645 G9.",
    "If wake-word detection now passes: write phase_completion_records/P002-completion.md and mark roadmap.md's Phase 2 status 'verified', then proceed to Phase 3 (Multi-AI Brain).",
    "If it still fails: use the new per-model score diagnostics (printed even on failure) to determine whether it's a threshold-tuning issue (scores close to 0.5) or a deeper capture issue (no scores/near-zero scores), and report back for further investigation."
  ],
  "last_verified_commit": null
}
